export type VehicleErrorProps = {
  workspaceId: string;
};

export type TimelineEmptyProps = {
  isSold: boolean;
  onAction: () => void;
};

export type TabsErrorProps = {
  onAction: () => void;
};

export type ReminderEmptyProps = {
  isSold: boolean;
  onAction: () => void;
};

export type MaintenanceEmptyProps = {
  isSold: boolean;
  onAction: () => void;
};

export type GalleryEmptyProps = {
  isSold: boolean;
  onAction: () => void;
};

export type TiresEmptyProps = {
  isSold: boolean;
  onAction: () => void;
};
